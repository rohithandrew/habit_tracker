import { ComingSoon } from '@/components/coming-soon';

export default function TimerScreen() {
  return (
    <ComingSoon
      emoji="⏱️"
      title="Focus timer"
      body="Start/stop sessions and a friend-facing dashboard are coming in a later phase."
    />
  );
}
